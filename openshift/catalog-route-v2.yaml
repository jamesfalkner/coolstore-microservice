apiVersion: config.istio.io/v1alpha2
kind: RouteRule
metadata:
  name: catalog-v2-all
spec:
  destination:
    name: catalog
  precedence: 1
  route:
  - labels:
      version: v2
    weight: 100
  - labels: 
      version: v1
    weight: 0
